---
import Icon from "./Icon.astro";
---

<aside class="brutal-card contact-cta">
  <div class="card-content">
    <p class="label">Available for work</p>
    <h2 class="cta-title">Need someone who owns the whole product?</h2>
    <p class="cta-lede">
      Full-time, contract, or rescue sprint — I take ownership from idea to production. Let's figure out what makes sense.
    </p>
    <div class="cta-row">
      <a href="/hire/" class="brutal-btn">
        HIRE ME
      </a>
      <a href="https://linkedin.com/in/moughamir" class="ghost-link">LINKEDIN</a>
    </div>
  </div>
</aside>

<style>
  .contact-cta {
    background: var(--gray-0);
    color: var(--gray-999);
    margin-top: 5rem;
  }

  .cta-title {
    font-size: clamp(2rem, 6vw, 4.5rem);
    line-height: 1.1;
    font-weight: 800;
    margin-bottom: 1.5rem;
    color: var(--gray-999);
  }

  .cta-lede {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--gray-300);
    max-width: 50ch;
    margin-bottom: 3rem;
  }

  .cta-row {
    display: flex;
    gap: 2rem;
    align-items: center;
  }

  .ghost-link {
    color: var(--gray-999);
    font-weight: 800;
    text-transform: uppercase;
    text-decoration: none;
    text-underline-offset: 0.3em;
    transition: color 0.1s;
  }

  .ghost-link:hover {
    color: var(--accent-regular);
    text-decoration: underline;
  }

  .label {
    color: var(--accent-regular);
    font-family: var(--font-body);
    font-weight: 800;
    text-transform: uppercase;
    margin-bottom: 1rem;
    display: block;
  }
</style>

